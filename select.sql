SELECT * -- devuelve la base de datos completa
SELECT * FROM users -- trae la tabla completa users
SELECT name FROM users --devuelve solo la fila name de la tabla users (idal para optimizar recursos y no solicitar informacion inesesaria)
SELECT name, user_id FROM users --devuelve esos dos filas de la tabla users
